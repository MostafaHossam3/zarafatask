import 'package:flutter/material.dart';

class CustomJsonImage extends StatelessWidget {
  const CustomJsonImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
